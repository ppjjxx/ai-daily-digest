# AI 大模型应用开发 · 每日速递

> 📅 2026年5月31日 · 星期日  
> 🎯 面向应届毕业生 | AI 应用开发岗 · 日积月累

---

## 🔥 热点速递

### 1. MiniMax 启动 A 股 IPO，"大模型双雄"会师科创板

MiniMax 于 5 月 29 日与中信证券签署辅导协议，正式启动科创板 IPO 进程。作为年初已在港交所上市的国内头部大模型厂商，MiniMax 港股上市以来涨幅超 4 倍，当前市值约 2636 亿港元。最新业务数据显示：全球用户约 **3 亿**，B 端企业和开发者客户突破 **100 万**（半年前仅 20 万，暴增 5 倍），年化经常性收入（ARR）过去两个月实现超 100% 增长，折年率逼近 **3 亿美元**量级。与此同时，智谱也在推进科创板 IPO。大模型从"烧钱圈地"正式进入"资本变现"阶段。

### 2. Anthropic 深陷"降速门"争议，Opus 4.8 仓促发布 Bug 不断

外媒爆料 Anthropic 在新旗舰 Claude Opus 4.8 发布前，疑似暗中削弱旧版模型性能，被用户称为 AI 版"降速门"。而 5 月 28 日仓促发布的 Opus 4.8 本身也 Bug 不断、Token 消耗飙升，引发开发者社区强烈不满。这场信任危机折射出一个核心矛盾：**当 AI 公司从"理想主义研究机构"转型为"商业化产品公司"，用户对模型行为的透明度和公平性要求急剧上升。**

### 3. 英伟达预告"PC 新时代"，或将进军 Windows PC 处理器市场

台北 Computex 大会前夕，英伟达官方社交账号发出三字预告——"A new era of PC"，并与微软 Windows 和 Arm 官方账号联动发布。多方信号表明：英伟达即将宣布进军 Windows PC 处理器市场，不再只卖 GPU。这对 AI 开发者意味着：**本地推理硬件的竞争格局可能被彻底改写**——未来个人 PC 上的 AI 推理能力可能大幅提升。

### 4. 复旦眸深智能发布 STI-WM：中国原生物理 AI 全球领跑

由复旦"铁三角"团队创立的眸深智能发布 STI-WM（时空一体世界动作模型），号称解决当前 VLA（视觉语言动作）模型空间感知精度不足、长时序规划能力薄弱等痛点，主打真正的"懂物理、会规划、能落地"的物理世界 AI 大脑。具身智能赛道正在从"能说会道"转向"能打能干"。

### 5. 40 余款 AI 大模型集中亮相 2026 世界智能产业博览会

天津智博会现场，"AI 大模型对话""生成式 AI 演示""AI 智能体应用"成为三大关注热点。与往年不同，本届大模型展览从"能力展示"真正迈向了"产业应用"——如基于大模型的汽车发动机维修预测与诊断系统，已经能实现不拆解发动机即可提前诊断故障。

---

## 🎯 今日面试真题

> 本期聚焦：Agent 架构与协议生态

### 1. ReAct 框架是什么？它解决了什么核心问题？（高频）

**答题要点：** ReAct = Reasoning + Acting。核心工作循环：Thought（推理）→ Action（调用工具）→ Observation（观察反馈）→ 循环直到任务完成。解决的痛点：纯推理模型容易"空想"偏离事实，ReAct 通过显式推理 + 工具反馈将推理锚定在真实环境中。举例：问"今天北京天气"时，ReAct 会先推理"我需要查天气 API"，然后调用工具，收到结果后再推理"该穿什么"。

### 2. MCP 和 Function Calling 有什么区别？（中高频）

**答题要点：** Function Calling 是模型层面的能力（模型说"我想调用这个函数"），MCP 是协议层面的标准（定义"如何发现、描述、调用工具"）。MCP 解决的是 N×M 问题——如果每个应用都要单独对接每个工具，复杂度爆炸。MCP 将 N 个应用 × M 个工具的集成问题简化为 N+M。MCP 提供工具发现（`/tools/list`）、标准化接口描述（JSON Schema）、统一调用方式。

### 3. Agent 的记忆系统怎么设计？（中高频）

**答题要点：** 三层架构——工作记忆（当前任务轨迹和关键结论，类比人脑"正在想的事"）、会话记忆（摘要滚动，避免上下文过长，类比"今天聊了什么"）、长期记忆（向量检索/结构化存储历史信息，类比"知识储备"）。写入要点：区分"事实"与"推断"，附带时间戳和来源。技术栈：向量数据库（Chroma/Milvus）+ 摘要模型 + 结构化存储。

### 4. 向量数据库和传统数据库的核心区别是什么？什么时候用向量数据库？

**答题要点：** 传统数据库基于精确匹配（SQL `WHERE name = '张三'`），向量数据库基于语义相似度（"找和这段文字意思最接近的 5 条记录"）。向量数据库本质是把文本、图片等非结构化数据转成高维向量，再用近似最近邻（ANN）算法做相似度检索。当你的应用需要"语义搜索"而非"关键词匹配"时用向量数据库——比如 RAG 的文档检索、推荐系统的相似内容推荐、以图搜图等。

### 5. Agent 中规划（Planning）有哪些主流策略？（中高频）

**答题要点：** 两大路线——① ReAct / Tool-use（边推理边行动，适合简单任务）；② Plan-then-Execute（先制定完整计划再逐步执行，适合复杂任务）。Plan-then-Execute 的优势是减少中间步骤的 Token 消耗和延迟，但弱点在于计划可能不够灵活、遇到意外情况难以调整。实际工程中常用混合策略：先做粗粒度规划，每个步骤内部用 ReAct 动态调整。

### 6. 在 RAG 系统中，为什么要对长文档做切片（Chunking）？切片重叠（Overlap）有什么用？

**答题要点：** 切片原因：① Embedding 模型有最大输入长度限制（如 512 token）；② 切片后可检索最相关片段而非整个文档；③ 单片段信息更聚焦，向量质量更高。重叠作用：确保上下文连续性，避免信息在切片边界处丢失。如果一段关键信息正好被切成两半，没有重叠就可能漏掉。通常设 10%~20% 的重叠比例。

### 7. A2A（Agent-to-Agent）协议和 MCP 是什么关系？各解决什么问题？

**答题要点：** MCP 解决"Agent 怎么调用工具"（纵），A2A 解决"Agent 之间怎么协作"（横），两者正交互补。MCP 是客户端-服务端架构，请求-响应模式；A2A 是点对点架构，支持长时任务协作。A2A 的三大核心概念：Agent Card（能力发现，类似服务注册）、Task（任务生命周期管理）、Push Notifications（推送通知，无需轮询）。一句话：跟工具打交道用 MCP，跟其他 Agent 协作用 A2A。

### 8. 你在项目中遇到 RAG 效果不好的情况怎么排查？（开放题 / 项目经验题）

**答题要点：** 按链路逐层排查——① 检索层：看看是不是切片策略有问题（太大/太小？重叠不够？），召回率够不够（试试 Hybrid Search 混合检索）？② 排序层：向量相似度不等于语义相关性，可以加 Rerank 模型（如 Cohere Rerank / BGE-Reranker）；③ 生成层：是不是 Prompt 没写好？有没有要求模型"仅基于参考内容回答"？④ 源头层：知识库本身质量行不行？有没有过期内容？——面试官考察的是你的工程排查思维，不是具体答案。

---

## 📖 每日知识点

> 每天 5 个 AI/LLM 核心概念，通俗解释 + 面试怎么考，面向应届生日积月累

### 概念 1：ReAct 模式

**通俗解释：** 想象你请朋友帮你查天气——他不会直接乱猜，而是先想"我需要打开天气 APP"，然后打开 APP 查看，看到结果后再想"哦下雨了，那该带伞"。这个"想→做→看→再想"的循环就是 ReAct。在 AI 中，ReAct 让模型不只是"一次生成答案"，而是在推理和行动之间反复循环，每一步都根据上一步的结果决定下一步做什么。

**面试怎么考：** "ReAct 和 Chain-of-Thought 有什么区别？"——CoT 是纯推理链（只想不做），ReAct 是推理+行动交替（边想边做）。ReAct 多了工具调用和环境反馈，能处理需要"外部信息"的任务。

### 概念 2：MCP（Model Context Protocol）

**通俗解释：** 假设你的手机要连蓝牙耳机、WiFi、车载系统、智能家居……如果没有统一标准，每个设备都得单独适配，疯掉。MCP 就是 AI 世界的"蓝牙协议"——Anthropic 定义了一套标准，让任何 AI 应用都能用同一套方式连接任何外部工具（数据库、API、文件系统）。开发者只需要实现一个 MCP Server，所有支持 MCP 的 AI 客户端就都能用了。

**面试怎么考：** "你用过 MCP 吗？MCP Server 怎么实现？"——MCP Server 本质是一个进程，通过 JSON-RPC 2.0 暴露 `tools/list` 和 `tools/call` 两个核心端点。建议面试前至少手写一个简单的 MCP Server（如查询天气的 Server），会大大加分。

### 概念 3：向量数据库

**通俗解释：** 传统数据库存的是"名字=张三、年龄=25"这种精确数据，查的时候必须一模一样。向量数据库存的是"这段话的数学特征"（一串 768 维的数字），查的时候找"最像的"而不是"完全一样的"。举例：你搜"怎么炖牛肉"，传统数据库只能返回标题包含"炖牛肉"的文章，向量数据库能返回"红烧牛肉的做法""卤牛肉教程"这些语义相近但用词不同的内容。

**面试怎么考：** "Chroma 和 Milvus 有什么区别？什么时候选哪个？"——Chroma 轻量级，适合原型开发和本地调试；Milvus 生产级，支持分布式、十亿级向量。小项目用 Chroma，上生产用 Milvus。

### 概念 4：Agent 的规划能力

**通俗解释：** 人和大模型的本质区别之一：人会"先想好再动手"，大模型容易"想到哪做到哪"。Agent 的规划能力就是让 AI 学会"做计划"——面对一个复杂任务（如"帮我安排下周三去上海的行程"），Agent 不是直接乱调工具，而是先分解为"查航班→比价→订票→订酒店→排日程"的步骤，再逐步执行。两种主流方式：ReAct（边想边做，灵活但慢）和 Plan-then-Execute（先做计划再执行，高效但遇到意外难调整）。

**面试怎么考：** "你设计一个旅行规划 Agent，怎么处理'航班取消'这种意外？"——考察动态重规划能力。答案要点：在执行步骤中加入"检查点"，每个步骤执行后验证结果，如果异常则触发重规划（回到规划阶段，基于当前新状态重新制定剩余步骤）。

### 概念 5：Rerank（重排序）

**通俗解释：** 向量检索像"初筛简历"——从 10000 份简历中快速找出 20 份看起来相关的。Rerank 像"HR 终面"——拿着这 20 份简历仔细看一遍，重新排序，精确挑出最合适的 3 份。因为向量相似度只是数学上的"接近"，不代表语义上真的"相关"——两段话可能向量很接近但内容是互相矛盾的。Rerank 模型（如 BGE-Reranker、Cohere Rerank）会比向量检索更精细地判断"用户问题和文档片段是不是真正相关"。

**面试怎么考：** "Rerank 为什么不能替代向量检索？"——Rerank 精度高但速度慢、成本高。如果把 10000 条都交给 Rerank 模型跑一遍，延迟和费用都会爆炸。所以正确用法是：向量检索做粗筛（K=10~20），Rerank 做精选（K=3~5）。

---

## 🛠️ 技术深潜

> 本期主题：**从零手写一个 MCP Server**（Python 实战全流程拆解）

MCP 是 2026 年 AI 应用开发的"基础设施级"技能。面试和工作中，能说清楚 MCP 原理 + 能手写 MCP Server 是巨大加分项。我们用 Python 从零搭建一个"本地文件搜索 MCP Server"。

### 第一步：理解 MCP 的核心架构

MCP 的通信模型很简单：

```
AI 客户端（Claude Desktop / Cursor / 你的应用）
        │
        │ ① tools/list ──→ 问"你有哪些工具？"
        │ ② tools/call ──→ 说"帮我调用这个工具，参数是X"
        │
    MCP Server
        │
        │ ③ 本地/远程执行
        ↓
    返回结果
```

传输方式有两种：**Stdio**（标准输入输出，本地进程通信，最常用）和 **SSE/HTTP**（远程通信）。

### 第二步：项目结构

```
mcp-file-search/
├── server.py          # MCP Server 主逻辑
├── pyproject.toml     # Python 项目配置
└── README.md
```

### 第三步：核心代码

```python
# server.py — 一个能搜索本地文件的 MCP Server
import json
import sys
import os
from pathlib import Path

def handle_request(request):
    """处理 JSON-RPC 请求"""
    method = request.get("method")
    req_id = request.get("id")
    
    if method == "tools/list":
        return {
            "jsonrpc": "2.0",
            "id": req_id,
            "result": {
                "tools": [
                    {
                        "name": "search_files",
                        "description": "在指定目录中搜索文件名匹配的文件",
                        "inputSchema": {
                            "type": "object",
                            "properties": {
                                "directory": {
                                    "type": "string",
                                    "description": "要搜索的目录路径"
                                },
                                "pattern": {
                                    "type": "string",
                                    "description": "文件名匹配模式，支持通配符，如 *.py"
                                }
                            },
                            "required": ["directory", "pattern"]
                        }
                    },
                    {
                        "name": "read_file_content",
                        "description": "读取指定文件的前N行内容",
                        "inputSchema": {
                            "type": "object",
                            "properties": {
                                "filepath": {
                                    "type": "string",
                                    "description": "文件路径"
                                },
                                "lines": {
                                    "type": "integer",
                                    "description": "读取行数，默认50",
                                    "default": 50
                                }
                            },
                            "required": ["filepath"]
                        }
                    }
                ]
            }
        }
    
    elif method == "tools/call":
        params = request.get("params", {})
        tool_name = params.get("name")
        arguments = params.get("arguments", {})
        
        if tool_name == "search_files":
            directory = arguments.get("directory", ".")
            pattern = arguments.get("pattern", "*")
            try:
                matches = list(Path(directory).rglob(pattern))[:20]
                result_text = "\n".join(str(m) for m in matches) or "未找到匹配文件"
            except Exception as e:
                result_text = f"搜索出错: {str(e)}"
        
        elif tool_name == "read_file_content":
            filepath = arguments.get("filepath")
            lines = arguments.get("lines", 50)
            try:
                with open(filepath, "r", encoding="utf-8") as f:
                    content = "".join(f.readline() for _ in range(lines))
                result_text = content or "(文件为空)"
            except Exception as e:
                result_text = f"读取出错: {str(e)}"
        else:
            result_text = f"未知工具: {tool_name}"
        
        return {
            "jsonrpc": "2.0",
            "id": req_id,
            "result": {
                "content": [{"type": "text", "text": result_text}]
            }
        }
    
    else:
        return {
            "jsonrpc": "2.0",
            "id": req_id,
            "error": {"code": -32601, "message": f"未知方法: {method}"}
        }

def main():
    """主循环：从 stdin 读取 JSON-RPC 请求，处理后写入 stdout"""
    for line in sys.stdin:
        line = line.strip()
        if not line:
            continue
        try:
            request = json.loads(line)
            response = handle_request(request)
            sys.stdout.write(json.dumps(response) + "\n")
            sys.stdout.flush()
        except json.JSONDecodeError:
            pass

if __name__ == "__main__":
    main()
```

### 第四步：配置 Claude Desktop 使用你的 MCP Server

在 Claude Desktop 的配置文件中添加：

```json
{
  "mcpServers": {
    "file-search": {
      "command": "python",
      "args": ["/path/to/mcp-file-search/server.py"]
    }
  }
}
```

配置完成后重启 Claude Desktop，你就能在对话中让 Claude 帮你搜索本地文件了。

### 第五步：关键踩坑点

1. **JSON-RPC 是行分隔的**：每个请求/响应占一行，不要发多行 JSON
2. **Stdio 模式无状态**：Server 不会记住上次查询的结果，如果需要"有状态"（如多步骤工作流），要在应用层自己管理
3. **`inputSchema` 写得好不好直接影响可用性**：描述和参数说明越详细，AI 模型越能准确调用
4. **生产环境的 MCP Server** 建议使用官方 `mcp` Python SDK（`pip install mcp`），不用像上面这样手写 JSON-RPC 解析——但面试时能手写说明你真正理解了协议

### 面试加分点

如果面试官问"你了解 MCP 吗"，不要只说"知道，是个协议"。你应该说：

> "我不仅用过 MCP，还手写过 MCP Server。MCP 本质是 JSON-RPC 2.0 over Stdio/SSE，核心是两个端点 `tools/list` 和 `tools/call`。我用 Python 从零写过一个文件搜索 MCP Server，实际跑在 Claude Desktop 上。关键要理解 MCP 是无状态协议，多步骤工作流需要在应用层管理状态，这也是 MCP 1.0 正在通过 Stateful Sessions 解决的问题。"

---

## 📚 学习资源推荐

### 📝 最新面经 & 面试指南

| 资源 | 链接 | 推荐理由 |
|------|------|----------|
| JavaGuide AI 面试指南 | [javaguide.cn](https://javaguide.cn/ai/interview-questions/ai-interview-guide.html) | 最全面的 AI 面试题汇总，Agent/RAG/MCP 全覆盖 |
| 小林 Coding 面试笔记 | [xiaolincoding.com](https://xiaolincoding.com/project/xiaolinnote.html) | 2026 最新 Agent 面试题 + 详细答案 |
| 代码随想录大模型面经 | [programmercarl.com](https://programmercarl.com/qita/0022.llminterview.html) | 覆盖 Vibe Coding / Claude Code / 微调等新方向 |
| 腾讯云开发者社区 | [27道Agent面试真题](https://cloud.tencent.com.cn/developer/article/2668240) | 27 道大厂真题 + 答题框架 |

### 🔥 2026 年 GitHub 热门 AI 项目（适合练手）

| 项目 | Stars | 一句话说明 | 适合练什么 |
|------|-------|-----------|-----------|
| **LangGraph** | 10k+ | LangChain 出品，Agent 编排框架 | Agent 架构设计、状态机编程 |
| **CrewAI** | 25k+ | 多 Agent 协作框架 | Multi-Agent 系统设计 |
| **Dify** | 70k+ | 可视化 AI 应用开发平台 | RAG + Agent + 工作流 |
| **Ollama** | 130k+ | 本地运行大模型 | 模型部署、推理优化 |
| **Milvus** | 33k+ | 生产级向量数据库 | 向量检索、大规模 RAG |
| **MCP Python SDK** | 25k+ | Anthropic 官方 MCP 实现 | MCP Server/Client 开发 |

### 💡 本周练手项目建议

**项目：本地知识库问答机器人（3 天可完成）**

- Day 1：用 Ollama + nomic-embed-text 搭建本地 Embedding 服务
- Day 2：用 Chroma 做向量存储，LangChain 做 RAG 链路
- Day 3：加个 MCP Server 把知识库检索封装成标准工具，Claude Desktop 可调用

面试时能把这个项目讲清楚（为什么选 Ollama 而不是 API？Chunk 大小怎么定的？加了 Rerank 前后效果差多少？），RAG + MCP 两道大题就稳了。

---

## 💬 今日金句

> "写代码只是 AI 应用开发的第一步。真正拉开差距的，是你能不能在面试中，把一个开源 Demo 讲成一个有决策、有踩坑、有优化的工程故事。"

---

*文中所引用信息均来源于公开发布的新闻报道、技术博客及官方公告。本文为 AI 自动生成内容，仅供学习交流使用。*

---

> 🏷️ 本期标签：`AI Agent` `MCP` `A2A` `RAG` `ReAct` `向量数据库` `MiniMax IPO` `面试题`
